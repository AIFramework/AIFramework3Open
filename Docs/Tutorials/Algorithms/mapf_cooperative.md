# Кооперативный A*: WHCA* и HCA*

## Постановка задачи

Дан граф $G$ и $k$ агентов. Требуется найти бесконфликтные пути с использованием кооперативного подхода — агенты планируют с учётом путей, уже запланированных другими. Кооперативный A* основан на резервировании позиций во времени.

## Reservation Table

Таблица резервирования хранит занятые ячейки пространства-времени $(v, t)$, пройденные рёбра $(u \to v, t)$ и стоянки на целях — клетки, где агент остаётся навсегда начиная с момента $t_0$:

$$\text{RT} = \{(v, t)\} \cup \{(u \to v, t)\} \cup \{(v, [t_0, \infty))\}$$

Агент не может занять зарезервированную ячейку, пройти ребро навстречу зарезервированному переходу — это был бы обмен местами — и остановиться на цели, через которую кто-то пройдёт позже. Резервирования одних вершин недостаточно: оно пропускает обмен соседей и наезд на агента, раньше закончившего путь.

## HCA* (Hierarchical Cooperative A*)

1. Вычислить эвристики для каждого агента (обратный BFS от целей)
2. Для каждого агента по очереди найти путь с помощью пространственно-временного A*
3. Зарезервировать найденный путь в RT

Пространственно-временной A* работает в расширенном графе $G' = (V \times T, E')$, где рёбра соединяют состояния $(v, t)$ и $(v', t+1)$.

## WHCA* (Windowed HCA*)

Ограничивает горизонт планирования окном $w$ шагов:

- Каждый агент планирует только на $w$ шагов вперёд
- Периодически перепланирует по мере продвижения
- Значительно ускоряет планирование для большого числа агентов

### Параметр окна

$$f(n) = g(n) + h(n), \quad g(n) \leq w$$

## Сложность

На каждого агента: $O(bw \log(bw))$ для WHCA* (вместо полного A*), где $b$ — фактор ветвления, $w$ — размер окна.

## Ограничения

- Неполнота: при неудачном порядке агентов решение может не быть найдено. Тогда HCA* оставляет агента на старте, а WHCA* — на месте в текущем окне, и решение не проходит `IsValid`
- Неоптимальность: зависимость от порядка планирования. HCA* планирует первыми агентов с дальними целями

## API

Пространство имён `AI.Algorithms.MAPF`. Классы называются `HCA` и `WHCA`; размер окна задаётся в **конструкторе**, а не в `Solve()`.

| Член | Описание |
|------|----------|
| `HCA(GridMap map, List<MAPFAgent> agents)` | Hierarchical Cooperative A*: резервирование на весь горизонт |
| `WHCA(GridMap map, List<MAPFAgent> agents, int windowSize = 16)` | Оконный вариант: резервирование только на `windowSize` тактов |
| `.Solve()` | `MAPFSolution` |

Окно — компромисс: чем оно меньше, тем дешевле планирование, но тем чаще агенты заходят в тупики, которых не видели заранее.

Исходники: `src/AI.Algorithms/MAPF/HCA.cs`, `WHCA.cs`.

## Код

```csharp
using AI.Algorithms.MAPF;

var map = new GridMap(15, 15);
for (int y = 3; y < 12; y++) map.SetBlocked(7, y, true);

var agents = new List<MAPFAgent>();
for (int i = 0; i < 4; i++)
    agents.Add(new MAPFAgent
    {
        Id = i,
        StartX = 0, StartY = i * 3,
        GoalX = 14, GoalY = i * 3,
    });

var hca = new HCA(map, agents).Solve();
Console.WriteLine($"HCA*: makespan={hca.Makespan}, sumOfCosts={hca.SumOfCosts}");

// Сравнение окон: короткое окно дешевле, но качество решения падает
foreach (int w in new[] { 4, 8, 16, 32 })
{
    var sol = new WHCA(map, agents, windowSize: w).Solve();
    Console.WriteLine($"WHCA* (окно {w,2}): makespan={sol.Makespan}, " +
                      $"валидно={sol.IsValid(map, agents)}");
}
```

