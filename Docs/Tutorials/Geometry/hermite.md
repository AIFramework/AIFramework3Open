# Кусочно-кубический сплайн Эрмита

Сплайн Эрмита задаётся значениями функции и производных (касательных) в узлах.
Обеспечивает $C^1$-гладкость (непрерывность первой производной) на стыках.
Удобен, когда известны не только точки, но и направления (скорости) в них.

## Определение

На отрезке $[t_k, t_{k+1}]$ с нормализованным параметром $s = \frac{t - t_k}{t_{k+1} - t_k} \in [0, 1]$:

$$p(s) = h_{00}(s)\,p_k + h_{10}(s)\,m_k + h_{01}(s)\,p_{k+1} + h_{11}(s)\,m_{k+1}$$

где $p_k, p_{k+1}$ — значения в узлах, $m_k, m_{k+1}$ — касательные (масштабированные на длину интервала).

## Базисные функции Эрмита

$$h_{00}(s) = 2s^3 - 3s^2 + 1$$
$$h_{10}(s) = s^3 - 2s^2 + s$$
$$h_{01}(s) = -2s^3 + 3s^2$$
$$h_{11}(s) = s^3 - s^2$$

### Свойства базисных функций

| Функция | $f(0)$ | $f(1)$ | $f'(0)$ | $f'(1)$ |
|---------|--------|--------|---------|---------|
| $h_{00}$ | 1 | 0 | 0 | 0 |
| $h_{10}$ | 0 | 0 | 1 | 0 |
| $h_{01}$ | 0 | 1 | 0 | 0 |
| $h_{11}$ | 0 | 0 | 0 | 1 |

## Матричная форма

$$p(s) = \begin{pmatrix} s^3 & s^2 & s & 1 \end{pmatrix} \begin{pmatrix} 2 & -2 & 1 & 1 \\ -3 & 3 & -2 & -1 \\ 0 & 0 & 1 & 0 \\ 1 & 0 & 0 & 0 \end{pmatrix} \begin{pmatrix} p_k \\ p_{k+1} \\ m_k \\ m_{k+1} \end{pmatrix}$$

## Выбор касательных

- **Вручную**: дизайнер задаёт касательные.
- **Конечные разности**: $m_k = \frac{p_{k+1} - p_{k-1}}{2}$ → сплайн Catmull–Rom.
- **Из условия $C^2$**: даёт натуральный кубический сплайн.

## Числовые замечания

- При неравномерном шаге $h_k = t_{k+1} - t_k$ касательные масштабируются: $m_k \leftarrow h_k \cdot m_k$.
- Базисные функции — полиномы 3-й степени, вычисление — по схеме Горнера.

## API

Класс `AI.Geometry.HermiteSpline` — методы `Evaluate(double t)`, `AddPoint(Point p, Vector tangent)`.
